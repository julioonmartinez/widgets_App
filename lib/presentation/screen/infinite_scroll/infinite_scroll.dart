import 'package:flutter/material.dart';

class InfiniteScroll extends StatelessWidget {
  static const name = 'InfiniteScroll';

  const InfiniteScroll({super.key});
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
